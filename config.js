/**
 * 網站全局設定
 * 這裡可以調整後台路徑、店名等設定
 */
export const CONFIG = {
    // 後台管理的路徑 (建議修改為難猜的名稱)
    ADMIN_PATH: '/a',

    // 店名設定
    STORE_NAME: 'Xun.Store',
    STORE_ICON: 'C',

    // 其他設定可以在這裡擴充
};
